import { SettingsPage } from "@/components/settings-page";

export const metadata = {
  title: "Settings - PenguinX Market Engine",
};

export default function Settings() {
  return <SettingsPage />;
}
